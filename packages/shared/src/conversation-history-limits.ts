/** Placeholder title until the first user message is persisted. */
export const DEFAULT_CONVERSATION_THREAD_TITLE = 'Untitled conversation'

/** Default max rows for conversationHistory:list / search when limit is omitted. */
export const CONVERSATION_HISTORY_DEFAULT_LIST_LIMIT = 100

/** Upper bound aligned with conversationHistory:list/search IPC input schema. */
export const CONVERSATION_HISTORY_MAX_LIST_LIMIT = 500
